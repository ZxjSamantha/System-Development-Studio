version: 2

jobs:
  build:
    machine:
      image: windows.medium 
      steps:
        - checkout
        - run: Write-Host 'Hello, Windows'
        # - add_ssh_keys:
        #    fingerprints:
        #      - "2d:be:57:e7:a7:5a:53:24:b6:89:86:de:f2:13:cb:af"
        #- run: sh ./deploy.sh

